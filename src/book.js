export class book {
   constructor(
      author,
      title,
      numPages,
      wasRead
   ) {
      this.author = author;
      this.title = title;
      this.numPages = numPages;
      this.wasRead = wasRead;
   }
}
